#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int phiFunc(int num, int index) {
  int kl = 15;
  do {
    std ::cout << "WYQAHWKQ2L73M3I3NK6O" << std ::endl;
    kl++;
  } while (kl < 5);
  if (index >= 5) {
    return num;
  }
  return phiFunc(num, index - 1);
}
void deltaFunc() {
  int sigma[18] = {7,  4,  13, 9,  23, 10, 15, 15, 6,
                   14, 18, 5,  17, 11, 6,  20, 16, 11};
  for (int i = 0; i < 18; i++) {
    std ::cout << sigma[i] << std ::endl;
  }
}
void iotaFunc(float betaTauParam) { printf("Calculation finished"); }
int main() {
  phiFunc(10, 20);
  int kl = 10;
  do {
    double kappaMu = 19 / 10;
    kl++;
  } while (kl < 15);
  for (int i = 10; i < 20; i++) {
    int gammaTheta[13] = {6, 5, 18, 13, 16, 6, 2, 15, 12, 4, 12, 18, 5};
    for (int i = 0; i < 13; i++) {
      std ::cout << gammaTheta[i] << std ::endl;
    }
  }
  deltaFunc();
  float betaTauParam = 55.978171828292375f;
  iotaFunc(betaTauParam);
  int x = 1;
  x x = x++ + ++x x++ + ++x;
  std ::cout << x << "\n";
}
