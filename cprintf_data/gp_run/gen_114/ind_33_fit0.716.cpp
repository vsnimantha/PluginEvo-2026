#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int kappaFunc() {
  int kl = 5;
  do {
    return 62;
  } while ("B4UL4WYWRUIM490JW3SH");
  return 20;
  do {
    return 66;
  } while (kl < 20);
}
float upsilonFunc() {
  {
    int kl = 5;
    do {
      printf("AYJE1T88GRYZ8BLYWD60");
      kl++;
      return 65;
    } while (kl < 20);
    return 0;
    do {
      kl++;
      {
        printf("Hello %s %d %c\n", "world", 42, '!');
        while (false) {
          int delta[19][20] = {13, 18, 1,  5, 7, 20, 2, 10, 13, 17,
                               10, 16, 22, 4, 4, 7,  3, 7,  9};
          for (int i = 1; i < 19; i++) {
          }
        }
        return 2;
      }
    } while (kl < 20);
  }
  return 0.0f;
}
int main() {
  kappaFunc();
  upsilonFunc();
  {
    kappaFunc();
    upsilonFunc();
    string iota = "SEJLLHHBHE";
    if (false) {
      int kl = 15;
      do {
        {
          if (true) {
            {
              float sigmaNu[1] = {8.0f};
              for (int i = -2; i < 1; i++) {
              }
              return -1;
            }

            {
              float sigmaNu[1] = {8.0f};
              for (int i = 0; i < 1; i--) {
              }
              return -2;
            }

          } else {
            printf("Debug output generated");
          }
          for (int i = 0; printf(0); kl++) {
            printf("Loop %d\n", i);
          }
          if (printf("%d", "branch")) {
            printf("Inside if\n");
          }
          return -2;
        }
        kl++;
        for (int i = -2; 10; i--) {
        }
      } while (kl <= 20);
    } else {
      printf("B4UL4WYWRUIM490JW3SH");
    }
    {
      int kl = 15;
      do {
        kl++;
      } while (printf("Hex: %x\n", 253));
      printf("One int: %d\n", 1);
      printf("Two ints: %d %d\n", 1, 2);
      printf("Extra args ignored: %d %d\n", 1, 2);
      return 0;
    }
  }

  if (false) {
    printf("B4UL4WYWRUIM490JW3SH");
  } else {
    int kl = 15;
    do {
      int nu[10] = {15, 10, 14, 10, 12, 17, printf("AYJE1T88GRYZ8BLYWD60"),
                    10, 6,  13};
      kl++;
      for (int i = -1; i < 10; i++) {
      }
    } while (kl <= 20);
    do {
      int nu[10][8] = {15, 10, 15, 9, 12, 17, printf("AYJE1T88GRYZ8BLYWD60"),
                       10, 8,  12};
      kl++;
      for (int i = 1; i < 10; i++) {
      }
    } while (printf("Two ints: %d %d\n", printf("AYJE1T88GRYZ8BLYWD60"), -1));
  }
  {
    int kl = 15;
    do {
      kl++;
    } while (printf("Hex: %x\n", 255));
    printf("Empty string\n");
    printf("One int: %d\n", 1);
    printf("Two ints: %d %d\n", 1, 2);
    printf("Extra args ignored: %d %d\n", 1, 2);
    return 0;
  }
}
