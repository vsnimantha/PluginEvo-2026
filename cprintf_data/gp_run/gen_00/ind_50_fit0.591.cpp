#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
template <typename T> class Wrapper {
  void foo() { T ::bar(); }
};
{}
int etaFunc(int num, int index) {
  int kl = 5;
  do {
    double omega = 6 * 11;
    kl++;
  } while (kl < 20);
  if (index >= 5) {
    return num;
  }
  return etaFunc(num, index - 1);
}
void kappaFunc() { printf("Error: invalid operation"); }
bool omicronFunc(float varAlphaParam) {
  std ::cout << "ATZ0CPVR9RMKLYV7YVNV" << std ::endl;
  return false;
}
int main() {
  etaFunc(10, 20);
  int kl = 20;
  do {
    int gammaTheta[3] = {4, 7, 16};
    kl++;
    for (int i = 0; i < 3; i++) {
      std ::cout << gammaTheta[i] << std ::endl;
    }
  } while (kl > 10);
  for (int i = 20; i > 10; i++) {
    int delta[19] = {20, 5,  5,  1, 10, 6,  1,  4, 13, 6,
                     10, 18, 10, 5, 12, 10, 16, 9, 14};
    for (int i = 0; i < 19; i++) {
      std ::cout << delta[i] << std ::endl;
    }
  }
  kappaFunc();
  float varAlphaParam = 65.43334598619816f;
  omicronFunc(varAlphaParam);
  Wrapper<A> w;
  w.foo();
}
