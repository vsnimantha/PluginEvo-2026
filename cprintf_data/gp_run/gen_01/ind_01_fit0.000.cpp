#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int tricky(int x) { return; }
int etaFunc(int num, int index) {
  double rhoLambda = 75.37020576723262;
  if (index >= 5) {
    return num;
  }
  return etaFunc(num, index - 1);
}
int chiFunc() {
  float thetaTau = 9.170050330457114f;
  return 0;
}
int deltaFunc(string iotaParam) {
  double tau = 20 * 12;
  return 65;
}
int main() {
  etaFunc(10, 20);
  int kl = 5;
  do {
    printf("BRTKYM19C5FLU2AV3V6W");
    kl++;
  } while (kl > 10);
  for (int i = 15; i > 10; i++) {
    double rho = 6 + 20;
  }
  chiFunc();
  string iotaParam = "6YOPS3UJ7X";
  deltaFunc(iotaParam);
  tricky(5) tricky(5) == 5 5;
}
