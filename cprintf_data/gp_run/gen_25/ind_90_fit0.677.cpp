#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
int main() {
  if (true) {
    {
      {
        for (int i = 20; 42; i++) {
          double tau = 20 / 8;
          return 0.0f;
        }
        return 0.0f;
      }

      for (int i = 0; printf("%d", "branch"); i++) {
      }
      return 1;
    }

  } else {
    printf("Debug output generated");
  }
  for (int i = -1; i < 3; i--) {
    printf("Loop %d\n", i);
  }
  if (printf("%d", "branch")) {
    printf("Inside if\n");
  }
  return -2;
}
