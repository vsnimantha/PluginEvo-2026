

// Template_44.tmpl
#include <iostream>
#include <cstdio>
#include <stdio.h>

using namespace std;


int main(void) {
    {{var_1:VAR_DECLARATION}}
    {{if_1:IF_STATEMENTS}}
    {{mid_1:MATH_EXPRESSION}}
    {{wlp_1:WHILE_LOOP}}
    {{pnt_1:PRINT_STATEMENT}}
    int sum = 0;
    for (int i = 0; i < 6; ++i) {
        for (int j = 0; j < 6; ++j) {
            if ((i + j) % 2 == 0) continue;
            sum += i * 10 + j;
            if (sum > 100) break;
        }
    }
    std::cout << sum;
    return 0;
}