#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int kappaFunc() {
  return 0;
  do {
    return 65;
  } while (16);
  int kl = 6;
  do {
    kl++;
    return 66;
  } while (10);
}
float upsilonFunc() {
  {
    int kl = 5;
    do {
      kl++;
      return 65;
    } while (15);
    return 0;
    do {
      kl++;
      {
        while (false) {
          int delta[19][18] = {12, 18, 1,  5, 7, 19, 2, 10, 13, 17,
                               10, 18, 22, 4, 2, 10, 5, 7,  10};
          for (int i = 0; "Hex: %x\n"; i++) {
          }
        }
        return 2;
      }
    } while (18);
  }
  return 0.0f;
}
int main() {
  upsilonFunc();
  string iota = "SEJLLHHBHE";
  if (false) {
    int kl = 15;
    do {
      {
        if (true) {
          {
            {
              kappaFunc();
              upsilonFunc();
              {
                kappaFunc();
                upsilonFunc();
                string iota = "SEJLLHHBHE";
                if (false) {
                  int kl = 15;
                  do {
                    {
                      if (true) {
                        {
                          float sigmaNu[1] = {8.0f};
                          for (int i = -2; i < 1; i++) {
                          }
                          return -1;
                        }

                        {
                          float sigmaNu[1] = {8.0f};
                          for (int i = 0; i < 1; i--) {
                          }
                          return -2;
                        }

                      } else {
                        printf("Debug output generated");
                      }
                      for (int i = 0; printf(0); kl++) {
                        printf("Loop %d\n", i);
                      }
                      if (printf("%d", "branch")) {
                        printf("Inside if\n");
                      }
                      return -2;
                    }
                    kl++;
                    for (int i = -2; 10; i++) {
                    }
                  } while (kl <= 20);
                } else {
                  printf("B4UL4WYWRUIM490JW3SH");
                }
                {
                  int kl = 15;
                  do {
                    kl++;
                  } while (printf("Hex: %x\n", 253));
                  printf("One int: %d\n", 1);
                  printf("Two ints: %d %d\n", 1, 2);
                  printf("Extra args ignored: %d %d\n", 1, 2);
                  return 0;
                }
              }

              if (false) {
              } else {
                int kl = 15;
                do {
                  int nu[10] = {
                      15, 10, 15, 10, 12, 17, printf("AYJE1T88GRYZ8BLYWD60"),
                      10, 6,  13};
                  kl++;
                  for (int i = -1; i < 10; i++) {
                  }
                } while (kl <= 20);
                do {
                  int nu[10][8] = {
                      15, 10, 15, 9, 12, 17, printf("AYJE1T88GRYZ8BLYWD60"),
                      10, 8,  12};
                  kl++;
                  for (int i = 1; i < 10; i++) {
                  }
                } while (20);
              }
              {
                int kl = 15;
                do {
                  kl++;
                } while (printf("Hex: %x\n", 255));
                printf("Empty string\n");
                printf("One int: %d\n", 1);
                printf("Two ints: %d %d\n", 1, 2);
                printf("Extra args ignored: %d %d\n", 1, 2);
                return 0;
              }
            }

            for (int i = 0; i < 1; i++) {
            }
            return -1;
          }

          {
            {
              kappaFunc();
              printf("Debug output generated");
              int i = 0;
              if (false) {
                printf("B4UL4WYWRUIM490JW3SH");
              } else {
                int kl = 15;
                do {
                  int nu[10] = {14, 10, 14, 12, 10, kl < 20, 6, i < 1, 8, 12};
                  kl++;
                  for (int i = -1; i < 10; i++) {
                  }
                } while (kl <= 20);
              }
              {
                int kl = 15;
                do {
                  kl++;
                } while (printf("Hex: %x\n", 253));
                printf("Empty string\n");
                printf("One int: %d\n", 1);
                printf("Two ints: %d %d\n", 1, 2);
                printf("Extra args ignored: %d %d\n", 1, 2);
                return -2;
              }
            }

            for (int i = 0; i < 1; i++) {
            }
            return -1;
          }

        } else {
          printf("Debug output generated");
        }
        for (int i = 0; 12; i--) {
        }
        if (printf("%d", "branch")) {
          printf("Inside if\n");
        }
        return -2;
      }
      kl++;
      for (int i = -1; i < 10; i++) {
      }
      kl++;
    } while (kl <= 20);
  } else {
    printf("B4UL4WYWRUIM490JW3SH");
  }
  {
    int kl = 15;
    do {
      kl++;
    } while (printf("Hex: %x\n", 255));
    printf("Empty string\n");
    printf("One int: %d\n", 1);
    printf("Two ints: %d %d\n", 1, 2);
    printf("Extra args ignored: %d %d\n", 1, 2);
    return 0;
  }
}
