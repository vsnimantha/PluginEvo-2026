// Template_8.tmpl
#include <iostream>
#include <stdio.h>

using namespace std;

// Handler for %s
void putstring(const char *s) {
    fputs(s, stdout);
}


int main(void) {
    printf("Hello %s %d %c\n", "world", 42, '!');
        int kl = 10 ; do { std::cout<< "Default output text" <<std::endl; kl ++; } while ( kl< 20 );
    return 0;
}