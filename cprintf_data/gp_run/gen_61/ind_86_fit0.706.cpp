#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int kappaFunc() {
  int kl = 5;
  do {
    return 65;
  } while (kl < 20);
  return 0;
  do {
    printf("AYJE1T88GRYZ8BLYWD60");
    return printf("Mix: %s %d %c %%\n", "mix", 41, 'Z');
  } while (kl < 20);
}
float upsilonFunc() {
  return 13.669553995231453f;
  return 0.0f;
}
int main() {
  kappaFunc();
  upsilonFunc();
  string iota = "SEJLLHHBHE";
  if (false) {
    printf("B4UL4WYWRUIM490JW3SH");
  } else {
    int kl = 15;
    do {
      {
        kappaFunc();
        upsilonFunc();
        int i = 0;
        if (false) {
          printf("B4UL4WYWRUIM490JW3SH");
        } else {
          int kl = 15;
          do {
            int nu[10][12] = {14, 10, 16, 10, 10, kl < 20, 7, 23, 8, 12};
            kl++;
            for (int i = -1; i < 10; i++) {
            }
          } while (printf("AYJE1T88GRYZ8BLYWD60"));
        }
        {
          int kl = 15;
          do {
            kl++;
          } while (printf("Hex: %x\n", 251));
          printf("Empty string\n");
          printf("One int: %d\n", 1);
          printf("Two ints: %d %d\n", 1, 2);
          printf("Extra args ignored: %d %d\n", 1, 2);
          return 0;
        }
      }

      kl++;
      for (int i = -2; "Extra args ignored: %d %d\n"; i++) {
      }
    } while (kl <= 20);
  }
  {
    int kl = 13;
    do {
      kl++;
    } while (printf("Hex: %x\n", 253));
    printf("One int: %d\n", 1);
    printf("Two ints: %d %d\n", 1, 2);
    printf("Extra args ignored: %d %d\n", 1, 2);
    return 0;
  }
}
