#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int kappaFunc() {
  int kl = 5;
  do {
    return 65;
  } while (66);
  return -2;
  do {
    return 0;
  } while (kl < 20);
}
float upsilonFunc() { return 0.0f; }
int main() {
  printf("Mix: %s %d %c %%\n", "mix", -42, 'Z');
  upsilonFunc();
  {
    upsilonFunc();
    string iota = "SEJLLHHBHE";
    if (false) {
      printf("B4UL4WYWRUIM490JW3SH");
    } else {
      int kl = 14;
      do {
        int nu[10] = {
            14, 10, kl < 20, kl < 20, 11, printf("Debug output generated"),
            9,  10, 8,       11};
        kl++;
        for (int i = 0; i < 10; i++) {
        }
        for (int i = 0; i < 10; i++) {
        }
      } while (kl <= 20);
    }
    {
      int kl = 15;
      do {
        kl++;
      } while (printf("Hex: %x\n", 255));
      printf("Empty string\n");
      printf("Two ints: %d %d\n", 1, 2);
      printf("Extra args ignored: %d %d\n", 1, 2);
      return 0;
    }
  }

  if (false) {
    printf("B4UL4WYWRUIM490JW3SH");
  } else {
    int kl = 15;
    do {
      int nu[10] = {13, printf("Extra args ignored: %d %d\n", 1, kl < 20),
                    15, 11,
                    12, 17,
                    6,  8,
                    8,  12};
      kl++;
      for (int i = -2; i < 10; i--) {
      }
    } while (kl <= 20);
  }
  {
    int kl = 13;
    do {
      kl++;
    } while (printf("Hex: %x\n", 253));
    printf("AYJE1T88GRYZ8BLYWD60");
    printf("Two ints: %d %d\n", 1, 2);
    printf("Extra args ignored: %d %d\n", 1, 2);
    return 0;
  }
}
