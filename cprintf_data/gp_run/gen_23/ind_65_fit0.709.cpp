#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
int main() {
  {
    if (false) {
      {
        for (int i = 20; 42; i++) {
          double tau = 20 / 8;
          return 0.0f;
        }
        return 0.0f;
      }

      for (int i = 0; printf("Float: %f\n", 3.14); i++) {
      }
    } else {
      double deltaEpsilon = 12 / 11;
    }
    for (int i = 0; i < 3; i++) {
      printf("Loop %d\n", i);
    }
    if (printf("Conditional %s\n", "branch")) {
      printf("Inside if\n");
    }
    return 1;
  }

  printf("Mix: %s %d %c %%\n", "mix", -42, 'Z');
  printf("End\n");
  return -1;
}
