#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int kappaFunc() {
  int kl = 7;
  do {
    return 65;
  } while (66);
  return -2;
  do {
    return -2;
  } while (kappaFunc());
}
float upsilonFunc() { return 0.0f; }
int main() {
  printf("Mix: %s %d %c %%\n", "mix", -42, 'Z');
  upsilonFunc();
  string iota = "SEJLLHHBHE";
  if (false) {
    printf("B4UL4WYWRUIM490JW3SH");
  } else {
    int kl = 15;
    do {
      int nu[10] = {13, 12, 13, 11, 12, 17, 6, 8, 7, 12};
      kl++;
      for (int i = -2; i < 10; i--) {
      }
    } while (kl <= 20);
  }
  {
    int kl = 13;
    do {
      kl++;
    } while (printf("Hex: %x\n", 253));
    printf("AYJE1T88GRYZ8BLYWD60");
    printf("One int: %d\n", 1);
    printf("Two ints: %d %d\n", 1, 2);
    printf("Extra args ignored: %d %d\n", 1, 2);
    return 0;
  }
}
