// Template_39.tmpl
#include <iostream>
#include <stdlib.h>
#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;

{{fid_2:FUNCTION_DEFINITION_RECURSIVE}}
{{fid_3:FUNCTION_DEFINITION}}
{{fid_4:FUNCTION_DEFINITION_PARAM}}


int main()
{
    {{fid_2:FUNCTION_CALL}}
    {{dwhl_1:DO_WHILE_LOOP}}
    {{flp_1:FOR_LOOP}}

    {{fid_3:FUNCTION_CALL}}
    {{fid_4:FUNCTION_CALL}}

std::vector<int> v = {5, 1, 8, 3, 7, 2, 6, 4};

    auto mid = std::partition(v.begin(), v.end(), [](int x) {
        return x < 5;
    });

    int checksum = 0;
    int idx = 0;
    for (auto it = v.begin(); it != mid; ++it, ++idx) {
        checksum = checksum * 11 + *it + idx;
    }
    for (auto it = mid; it != v.end(); ++it, ++idx) {
        checksum = checksum * 11 + *it + idx * 2;
    }

    std::cout << checksum;
    return 0;

}