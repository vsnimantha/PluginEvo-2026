#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
template <typename T> class Wrapper {
  void func() { typename T::Nested n; }
};
int sigmaFunc(int num, int index) {
  int kl = 5;
  do {
    float xiDelta[5] = {8.0f, 10.0f, 15.0f, 1.0f, 9.0f};
    kl++;
    for (int i = 0; i < 5; i++) {
      std ::cout << xiDelta[i] << std ::endl;
    }
  } while (kl < 20);
  if (index >= 5) {
    return num;
  }
  return sigmaFunc(num, index - 1);
}
double kappaFunc() {
  printf("JP380TI49F9ALKB0C2US");
  return 93.19934194078179;
}
double rhoFunc(bool chiParam) {
  double tauPi = 4 / 1;
  return 74.3303579834507;
}
int main() {
  sigmaFunc(10, 20);
  int kl = 5;
  do {
    printf("WME4EBHTPLITMCUMRXPT");
    kl++;
  } while (kl <= 20);
  for (int i = 5; i < 20; i++) {
    float omegaSigma[20] = {
        18.0f, 14.0f, 17.0f, 13.0f, 2.0f, 4.0f, 14.0f, 58.36495825274376f,
        8.0f,  11.0f, 17.0f, 4.0f,  3.0f, 6.0f, 12.0f, 2.0f,
        3.0f,  17.0f, 2.0f,  12.0f};
    for (int i = 0; i < 20; i++) {
      std ::cout << omegaSigma[i] << std ::endl;
    }
  }
  kappaFunc();
  bool chiParam = true;
  rhoFunc(chiParam);
  Wrapper<A> w;
  w.func();
}
