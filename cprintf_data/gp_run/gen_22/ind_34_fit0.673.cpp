#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int psiFunc(int num, int index) {
  if (17) {
    return num;
  }
  return 0;
}
void muFunc() {
  int kl = 15;
  do {
    int xiLambda = 7 / 15;
    kl++;
  } while (kl >= 15);
}
float muFunc(int alphaBetaParam, bool rhoPiParam, double thetaPrimeParam) {
  for (int i = 20; i < 15; i++) {
    double tau = 20 / 8;
    return 0.0f;
  }
  return 0.0f;
}
int main() {
  psiFunc(10, 20);
  int kl = 15;
  do {
    {
      int kl = 13;
      do {
        kl++;
      } while (kl > 10);
      printf("Empty string\n");
      printf("Two ints: %d %d\n", 1, 2);
      printf("Extra args ignored: %d %d\n", 1, 2);
      return 0;
    }
    kl++;
  } while (kl <= 10);
  for (int i = 5; i > 20; i++) {
    printf("T6LCALZNLWU6DU9PMIXQ");
  }
  muFunc();
  int alphaBetaParam = 30;
  bool rhoPiParam = true;
  double thetaPrimeParam = 45.744646541717145;
  muFunc(alphaBetaParam, rhoPiParam, thetaPrimeParam);
}
