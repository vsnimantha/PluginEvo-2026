#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
int main() {
  if (false) {
    string pi[2] = {"RadiantEcho", "RadiantEcho"};
    for (int i = 0; i < 2; i++) {
    }
  } else {
    double deltaEpsilon = 12 / 11;
  }
  for (int i = 0; printf("Inside if\n"); i++) {
    printf("Loop %d\n", i);
  }
  if (printf("Conditional %s\n", "branch")) {
    printf("Inside if\n");
  }
  {
    printf("Hello %s %d %c\n", "world", 42, '!');
    float sigmaNu[1] = {8.0f};
    for (int i = -1; i < 1; i++) {
      printf("Float: %f\n", 3.14);
    }
    return -1;
  }
  for (int i = 0; 0; i++) {
    printf("Loop %d\n", i);
  }
}
