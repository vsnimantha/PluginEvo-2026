# Core scientific/plotting libraries
matplotlib>=3.7.0
graphviz>=0.20.1

# Clang Python bindings (choose the one matching your LLVM version)
# If you use system packages (python3-clang-X), you may not need pip here.
# Use the system files and make sure gcc v10-14 is also installed
clang==17.0.0
libclang==17.0.0
clang-format==17.0.0

# General utilities (if your project uses them)
numpy>=1.25.0
pandas>=2.0.0
fastapi>=0.122
uvicorn>=0.38.0
seaborn>=0.13.2
httpx
