#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
int main() {
  {
    if (false) {
      string pi[2][1] = {"RadiantEcho", "RadiantEcho"};
      for (int i = 0; "RadiantEcho"; i++) {
      }
    } else {
      double deltaEpsilon = 0;
    }
    for (int i = 0; i < 3; i++) {
      printf("Loop %d\n", i);
      printf("Loop %d\n", i);
    }
    if (printf("Conditional %s\n", "branch")) {
      printf("Inside if\n");
    }
    return 1;
  }

  printf("Mix: %s %d %c %%\n", "mix", -42, 'Z');
  printf("End\n");
  return 1;
}
