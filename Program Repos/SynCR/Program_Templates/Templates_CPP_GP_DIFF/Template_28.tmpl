// Template_28.tmpl
#include <iostream>
#include <stdlib.h>
#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;

{{fid_2:FUNCTION_DEFINITION_RECURSIVE}}
{{fid_3:FUNCTION_DEFINITION}}
{{fid_4:FUNCTION_DEFINITION_PARAM}}


int main()
{
      {{fid_2:FUNCTION_CALL}}
    {{dwhl_1:DO_WHILE_LOOP}}
    {{flp_1:FOR_LOOP}}

    {{fid_3:FUNCTION_CALL}}
    {{fid_4:FUNCTION_CALL}}

std::unordered_map<int, int> um;
    um[1] = 10;
    um[4] = 5;
    um[1] += 3;
    um[7] = 2;

    std::vector<std::pair<int,int>> v(um.begin(), um.end());
    std::sort(v.begin(), v.end(), [](auto const &a, auto const &b) {
        return a.first < b.first;
    });

    int checksum = 0;
    for (auto const &p : v) {
        checksum = checksum * 19 + p.first * 4 + p.second;
    }
    std::cout << checksum;

    return 0
}
